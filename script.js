function alertFromInput() {
    let inputSearch = document.getElementById('inputSearch').value;
    if (inputSearch === 'google') {
        alert('яндекс круче, но это не точно')
    } else {
        alert(inputAreaValue);
    }

}